FROM mysql:8.0

# Set initial DB, user, and passwords (will be overridden by Render env vars if set)
ENV MYSQL_DATABASE=wordpress
ENV MYSQL_USER=wpuser
ENV MYSQL_PASSWORD=wppass
ENV MYSQL_ROOT_PASSWORD=rootpass

EXPOSE 3306
